<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="#ffffff"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/buttonNeg"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:text="-"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:background="@drawable/material_button"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/buttonPos"
        android:layout_toStartOf="@+id/buttonPos" />

    <Button
        android:id="@+id/buttonPos"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="10dp"
        android:layout_toLeftOf="@+id/locations_per_day"
        android:layout_toStartOf="@+id/locations_per_day"
        android:background="@drawable/material_button"
        android:text="+"
        android:textColor="#ffffff"
        android:textSize="20dp" />

    <EditText
        android:id="@+id/locations_per_day"
        android:layout_width="85dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_gravity="right"
        android:background="@drawable/material_button_blue"
        android:foregroundGravity="right"
        android:gravity="right|center_vertical"
        android:inputType="number"
        android:paddingRight="25dp"
        android:textColor="#ffffff"
        android:textSize="20dp" />
</RelativeLayout>